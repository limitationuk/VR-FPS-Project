using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;
using UnityEngine.XR.Interaction.Toolkit;

public class Magazine : MonoBehaviour
{
    //[Tooltip("")]

    //[SerializeField] int maxAmmo;
    //[SerializeField] int currentAmmo;
    //[SerializeField] bool isBeingGrabbed;
    //[SerializeField] Gun currentGun;
    //[SerializeField] WeaponType weaponType;
    //[SerializeField] new Collider collider;

    //[SerializeField] public Magazine startingMagazine;

    //[SerializeField] XRInteractionManager interactionManager;
    //[SerializeField] XRGrabInteractable xrGrabInteractable;

    // OnValidate : 다른 속성을 업데이트하거나 속성이 수정될 때마다 유효성 검사를 실행
    // 주의!! Inspector 창에서 스크립트의 속성이 수정될 때마다 'OnValidate'가 호출됨
    /*private void OnValidate()
    {
        if (!xrGrabInteractable)
            xrGrabInteractable = GetComponentInParent<XRGrabInteractable>();
        if (!interactionManager)
            interactionManager = FindObjectOfType<XRInteractionManager>();
    }*/

  
}
